// State Machine mockup — sequential state nodes and transition labels.
export const stateMachineTemplate = String.raw`<div class="diag-wrap mt-40">
  <div class="diag-state-machine">
    STATES_INJECT
  </div>
</div>`;
